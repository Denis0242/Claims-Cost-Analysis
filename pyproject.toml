[project]
name = "claims-cost-analysis"
version = "0.1.0"
description = "claims-cost-analysis"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "plotly>=6.7.0",
]
